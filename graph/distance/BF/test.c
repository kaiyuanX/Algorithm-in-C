#include "BF.h"
#include <stdio.h>
#include <stdlib.h>

int main()
{
    /* test 01 */
    // int vn = 7;

    // AdjList L = InitializeList(vn);
    // AddEdge(1, 2, 2, L);
    // AddEdge(3, 1, 4, L);
    // AddEdge(4, 3, 2, L);
    // AddEdge(1, 4, 1, L);
    // AddEdge(2, 4, 3, L);
    // AddEdge(4, 6, 8, L);
    // AddEdge(3, 6, 5, L);
    // AddEdge(2, 5, 10, L);
    // AddEdge(4, 5, 2, L);
    // AddEdge(4, 0, 4, L);
    // AddEdge(5, 0, 6, L);
    // AddEdge(0, 6, 1, L);

    // int *T = BellmanFord(L, 1);
    // for (int i = 0; i < vn; i++)
    // {
    //     printf("%d \n", T[i]);
    // }
    /* check */

    free(T);
    return 0;
}